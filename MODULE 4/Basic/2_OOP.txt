   what is OOP ?
 -> OOP - means Object Oriented Programming ,
          it is a way of writing programs that use of classes and objects
          to organize and structure code.
           
           
   List OOPs concepts.
 -> Class           : it is user defined data type,which holds its own data members and data function,
                      class is a blueprint representing a group of ojbect which shares some common properties.
                      when class is defined no memory is allocated.

 -> Objects         : to use the data and access function defined in class, we need objects.
 -> Encapsulation   :
 -> Abstraction     :
 -> Polymorphism    :
 -> Inheritance     :
 -> Dynamic Binding :   
 -> Message Passing :